class validator{
    static validUser(userData){
        if(userData.hasOwnProperty('name') &&
        userData.hasOwnProperty('email') &&
        userData.hasOwnProperty('password') &&
        userData.hasOwnProperty('preference')){
            
        }
    }
}